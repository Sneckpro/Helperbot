services:
  bot:
    build: .
    container_name: helperbot
    restart: unless-stopped
    env_file: .env
    environment:
      - DB_PATH=/app/data/notes.db
    volumes:
      - bot-data:/app/data

volumes:
  bot-data:
